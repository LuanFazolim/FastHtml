from setuptools import setup, find_packages

setup(
    name='FastHtml',
    version='1.0.0',
    packages=find_packages(),
    install_requires=[
        # Liste aqui as dependências do seu projeto
    ],
)
